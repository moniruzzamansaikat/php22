#extends('layouts/main')

#section('content')
    <h2>User List X</h2>
    <ul>
        #foreach ($users as $user)
            <li>{{ $user['username'] }}</li>
        #endforeach
    </ul>

    #if (count($users) === 0)
        <p>No users found.</p> 
    #endif
#endsection
